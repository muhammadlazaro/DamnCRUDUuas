version: '3'

services:
  web:
    build: .
    container_name: damncrud_app
    ports:
      - "8080:80"
    networks:
      - testnet

  selenium:
    image: selenium/standalone-chrome
    container_name: selenium
    ports:
      - "4444:4444"
    networks:
      - testnet

networks:
  testnet: